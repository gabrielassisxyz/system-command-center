> One line per completed task, appended by the loop. This is the trajectory log —
> "why did it do that on iteration 7?" is answered here.

- Snapshot data model — created `internal/model` with Snapshot, SystemSnapshot, ProcessRow, ContainerRow, ComposeGroup, and pointer-based metric structs; tests verify fully-populated and metrics-absent JSON serialization; `bin/ci` green.
- Loopback HTTP server + embedded static page — created `internal/server/static/index.html` and `app.js`, wired `embed` in `NewMux`, added `DefaultAddr`, and updated `cmd/hardware-usage/main.go` to default to `server.DefaultAddr()`; tests verify `GET /` returns the HTML body and the default address is `127.0.0.1:8765`; `bin/ci` green.
- SSE hub — added `internal/server/sse.go` with `SSEHub`, `SnapshotProvider`, and `EmptyProvider`; wired `/events` in `NewMux` with a ~2s production ticker; added `NewMuxWithHub` for tick-less tests; added `sse_test.go` with tests that two httptest clients receive a frame and that the provider is not called after all subscribers disconnect; `bin/ci` green.
- System metrics collector — created `internal/metrics` with a project-owned `SystemSource` interface, a `GopsutilSystemSource` production implementation, and a `SystemCollector` that builds `model.SystemSnapshot`; tests verify fully-populated values and absent-tolerant behaviour for CPU/RAM/temperature errors; `bin/ci` green.
- System I/O rate collector — created `internal/metrics/iorate.go` with a project-owned `IOSource` interface, `GopsutilIOSource` production implementation, and `IORateCollector` that diffs cumulative disk/network counters across `Collect()` calls using an injectable clock; tests cover first-call zero rates, delta/deltaT rate computation, new/removed devices, source errors leaving rates absent, and zero-delta guard; `bin/ci` green.

- Per-process collector — created `internal/metrics/process.go` with a project-owned `ProcessSource` interface, `GopsutilProcessSource` production implementation, and `ProcessCollector` that builds `[]model.ProcessRow`; tests with a fake source cover fully-populated rows, individual metric-read errors skipping the row, process-list errors returning empty, unnamed processes skipped, and zero-RSS omitted; `bin/ci` green.
- AMD GPU collector — created `internal/gpu/amd.go` with a sysfs `Reader` that discovers the first amdgpu card, reads `gpu_busy_percent`, `mem_info_vram_used/total`, and the matching amdgpu hwmon `temp1_input`; added `internal/gpu/amd_test.go` covering fully-populated, missing-file, partial-file, non-AMD, multi-card, wrong-hwmon-name, and zero-busy cases; `bin/ci` green.
- Per-process disk I/O — created `internal/metrics/procdisk.go` with a project-owned `ProcIOSource` interface, `ProcFSIOSource` production implementation, and `ProcessDiskIOCollector` that diffs `/proc/<pid>/io` cumulative read/write counters across `Collect()` calls using an injectable clock; tests cover first-call absent rates, delta/deltaT rate computation, permission-denied pids being absent without error, new/removed pids, zero-delta guard, and counter-reset guard; `bin/ci` green.
- Docker collector — rewrote `internal/docker/docker.go` to use plain HTTP calls to the local Docker Engine API behind a project-owned `DockerSource` interface, avoiding the `github.com/docker/docker` module and its govulncheck advisories. Added `internal/docker/types.go` with minimal API structs, and tests covering grouping, unlabeled buckets, list/stats errors, name stripping, and HTTP source behavior. `go test ./internal/docker/...` passes and `bin/ci` is green (including govulncheck).
- Snapshot assembler — created `internal/assembler/assembler.go` and `assembler_test.go`; `Assembler` combines system, I/O rate, per-process, per-process disk I/O, GPU, and Docker collectors into a `model.Snapshot` and satisfies the SSE `SnapshotProvider` interface; tests cover a complete snapshot and advancing rates on a second call. Re-ran `bin/ci` after fixing gofmt issue and defensive copies in `IORateCollector`/`ProcessDiskIOCollector`/`SystemCollector`; all gates green.
- Wired assembler into the SSE hub — updated `cmd/hardware-usage/main.go` to build a real `assembler.Assembler` from production sources (gopsutil system/process/I/O, `/proc` per-process I/O, sysfs GPU, Docker Engine API) and pass it to `server.NewMux`; added `cmd/hardware-usage/main_test.go` verifying the wiring succeeds and that an httptest `/events` client receives a fully populated Snapshot JSON frame with system, processes, and docker groups; `bin/ci` green.
- Kill action endpoint — added `POST /api/process/kill?pid=<n>` in `internal/server/actions.go` behind an injectable `ProcessKiller` interface; production `OsProcessKiller` in `internal/server/killer.go` sends SIGTERM; wired into `NewMux` and `NewMuxWithHub`; tests in `internal/server/actions_test.go` cover invoked-with-pid, missing pid, bad/empty pid, killer error as 5xx, process-not-found as 404, and wrong method; `bin/ci` green.
